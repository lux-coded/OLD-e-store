import React from 'react';
import { Paper, Stepper, Step, Label, Typography, CircularProgress, Divider, Button } from '@material-ui/core';

const Checkout = (props) => {
  return (
    <div>
      Checkout
    </div>
  );
}

export default Checkout;
